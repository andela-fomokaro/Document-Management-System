// import React from 'react';
// import PropTypes from 'prop-types';
// import { Link } from 'react-router';
// import { connect } from 'react-redux';

/* const DocumentBody = ({ document, serial }) => {
  return (
    <tr>
      <td> 1 </td>
      <td> documentTitle </td>
      <td> 2 </td>
      <td> 2017 </td>
    </tr>
  );
};*/
/*class DocumentBody extends React.Component {
  renderTable(doc) {
    return (
        <tr key={doc.content}>
        <td> doc.id </td>
        <td>doc.title</td>
        <td>doc.ownerId</td>
        <td> doc.updatedAt </td>
      </tr>
    )
  }
  render() {
    console.log(this.props.doc);
    const document = this.props.doc;
    return (
      this.props.doc.documents.map(this.renderTable)
    );
  }
}

const mapStateToProps = state => ({
  doc: state.documents
});

DocumentBody.propTypes = {
  doc: PropTypes.object.isRequired,
};

export default connect(mapStateToProps)(DocumentBody);*/
